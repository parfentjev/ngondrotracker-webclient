class Meditation {
  title: string;
  path: string;
  goal: number;

  constructor(title: string, path: string, goal: number) {
    this.title = title;
    this.path = path;
    this.goal = goal;
  }
}

export default Meditation;
